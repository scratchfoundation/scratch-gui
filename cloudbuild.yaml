steps:
- name: 'gcr.io/cloud-builders/npm'
  args: ['install']

- name: 'gcr.io/cloud-builders/npm'
  args: ['run', 'build']

- name: "gcr.io/cloud-builders/gcloud"
  entrypoint: bash
  args:
  - -c
  - |
    ln -sfnr build appengine/static
    gcloud app deploy -q appengine

timeout: "1600s"
